!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'New Gg'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'New Gg'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'
    %main{:role => "main"}
      %ul.bxslider
        %li 
          = image_tag "houses.jpg"
        %li 
          = image_tag "hillside.jpg"
      #content.pull-left
        = render 'layouts/messages'
        = yield
      #sidebar.pull-right.panel.panel-default
        .panel-heading
          %h3 Новости
        .panel-body
